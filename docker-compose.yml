

services:
  backend:
    image: chouchenechadha/covoiturage-backend:latest
    container_name: covoiturage-backend
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data: